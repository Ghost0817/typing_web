{% extends 'teacher.html.twig' %}

{% block title %}Teacher Log Up - Bicheech.com{% endblock title %}

{% block body %}
<div class="my_content">
    <h3 class="pageHeader">{%trans%}Create And Add Accounts{%endtrans%}<small> {%trans%}create or import your student roster{%endtrans%}</small></h3>

    {% for flash_message in app.session.flashbag.get('s_success') %}
        <div class="msg-success">
            {{ flash_message|raw }}
        </div>
    {% endfor %}

    {% for flash_message in app.session.flashbag.get('a_success') %}
        <div class="msg-success">
            {{ flash_message|raw }}
        </div>
    {% endfor %}

    {% for flash_message in app.session.flashbag.get('a_error') %}
        <div class="msg-error">
            {{ flash_message|raw }}
        </div>
    {% endfor %}

    {% if(s_form.username.vars.errors|length) %}
        <div class="msg-error">
            {{ form_errors(s_form.username) }}
        </div>
    {% endif %}

    <table width="100%" cellpadding="0" cellspacing="4">
        <tbody>
            <tr valign="top">
                <td width="33%">
                    <div class="panel panel-default">
                        <div class="panel-heading">{%trans%}Option{%endtrans%} 1 - {%trans%}Create Individual Student Accounts{%endtrans%}</div>
                        <div class="panel-body">
                        {{ form_start(s_form, {'action': path('usercreatepage'), 'method': 'POST', 'id': 'form_register', 'attr': {'autocomplete': 'off'}}) }}

                        <div class="form-item">
                            {{ form_label(s_form.username) }}
                            {{ form_widget(s_form.username, {'attr': {'class': 'form-control'}}) }}
                        </div>
                        <div class="form-item">
                            {{ form_label(s_form.password) }}
                            {{ form_widget(s_form.password, {'attr': {'class': 'form-control'}}) }}
                            {% if(s_form.password.vars.errors|length) %}
                                <div class="msg-error">{{ form_errors(s_form.password) }}</div>
                            {% endif %}
                        </div>
                        <div class="form-item">
                            {{ form_label(s_form.grade) }}
                            {{ form_widget(s_form.grade, {'attr': {'class': 'form-control'}}) }}
                            {% if(s_form.grade.vars.errors|length) %}
                                <div class="msg-error">{{ form_errors(s_form.grade) }}</div>
                            {% endif %}
                        </div>
                        <div class="form-item">
                            {{ form_label(s_form.firstname) }}
                            {{ form_widget(s_form.firstname, {'attr': {'class': 'form-control'}}) }}
                            {% if(s_form.firstname.vars.errors|length) %}
                                <div class="msg-error">{{ form_errors(s_form.firstname) }}</div>
                            {% endif %}
                        </div>
                        <div class="form-item">
                            {{ form_label(s_form.lastname) }}
                            {{ form_widget(s_form.lastname, {'attr': {'class': 'form-control'}}) }}
                            {% if(s_form.lastname.vars.errors|length) %}
                                <div class="msg-error">{{ form_errors(s_form.lastname) }}</div>
                            {% endif %}
                        </div>
                        <div class="form-item">
                            {{ form_label(s_form.email) }}
                            {{ form_widget(s_form.email, {'attr': {'class': 'form-control'}}) }}
                            {% if(s_form.email.vars.errors|length) %}
                                <div class="msg-error">{{ form_errors(s_form.email) }}</div>
                            {% endif %}
                        </div>

                        {{ form_errors(s_form) }}

                        {{ form_end(s_form) }}
                        </div>
                    </div>
                </td>
                <td width="34%">

                    <div class="panel panel-default">
                        <div class="panel-heading">{%trans%}Option{%endtrans%} 2 - {%trans%}Import Multiple Student Accounts{%endtrans%}</div>
                        <div class="panel-body">
                            <p>You can import any number of students at once using our Student Import Tool. This will allow you to quickly create any number of unique usernames and passwords.</p>
                            <img src="/teacher/images/excelFile.png" class="" style="margin: auto;display: block;width: 150px;" />
                            <a href="{{ path('usercreateimportpage') }}" class="btn btn-defualt">{%trans%}Go to Student Import Tool{%endtrans%} »</a>
                        </div>
                    </div>

                </td>
                <td width="33%">

                    <div class="panel panel-default">
                        <div class="panel-heading">{%trans%}Option{%endtrans%} 3 - {%trans%}Add Existing Accounts{%endtrans%}</div>
                        <div class="panel-body">
                            <p>{%trans%}If a student account was created outside of your Portal, you can add it to your portal here by entering the student's username and password below.{%endtrans%}</p>
                            <p>This allows you to add existing accounts, which were created either in a different portal, or registered manually using the typing tutor portion of Bicheech.com.</p>
                            {{ form_start(a_form, {'action': path('usercreatepage'), 'method': 'POST', 'id': 'form_register', 'autocomplete': 'off'}) }}

                            {{ form_errors(a_form) }}

                            <div class="form-item">
                                {{ form_label(a_form.username) }}
                                {{ form_widget(a_form.username, {'attr': {'class': 'form-control'}}) }}
                                {% if(a_form.username.vars.errors|length) %}
                                    <div class="msg-error">
                                        {{ form_errors(a_form.username) }}
                                    </div>
                                {% endif %}
                            </div>
                            <div class="form-item">
                                {{ form_label(a_form.password) }}
                                {{ form_widget(a_form.password, {'attr': {'class': 'form-control'}}) }}
                                {% if(a_form.password.vars.errors|length) %}
                                    <div class="msg-error">{{ form_errors(a_form.password) }}</div>
                                {% endif %}
                            </div>
                            <div class="form-item">
                                {{ form_label(a_form.grade) }}
                                {{ form_widget(a_form.grade, {'attr': {'class': 'form-control'}}) }}
                                {% if(a_form.grade.vars.errors|length) %}
                                    <div class="msg-error">{{ form_errors(a_form.grade) }}</div>
                                {% endif %}
                            </div>

                            {{ form_end(a_form) }}
                        </div>
                    </div>
                </td>
            </tr>
        </tbody>
    </table>
</div>
{% endblock %}

{% block stylesheets %}
<link rel="stylesheet" href="{{ asset('css/font-awesome.min.css') }}">
{% endblock %}

{% block javascripts %}
{% endblock javascripts %}
